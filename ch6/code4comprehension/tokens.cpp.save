#include "std_lib_facilities.h"

int main() {
  class Token {
  public:
      char kind;
      double value;
  Token (char ch)
      :kind (ch), value (0) {}
  Token (char ch, double val)
      :kind (ch), value (val) {}
  };

  Token ('p', 3.14);
  cout << "t1.kind = " << Token.kind << " and t1.value = " << t1.value << endl;
  return 0;
}
